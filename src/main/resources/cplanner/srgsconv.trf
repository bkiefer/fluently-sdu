<da>#da -> # ^ :#da ^ top, # ! <da>.

<prop>#prop -> # ^ (#prop), # ! <prop>.

<number>#n: -> # = string2number(#n), # ! <number>.

<neg> ^ <what>#w: -> # ^ <what>concatenate("-", #w), # ! <neg>.

(nom1:|raw:) ^ !<prop> ^ ! top -> # ^ top.
